import styled from 'styled-components';

export const FilterGridRow = styled.div`
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
`;

export const FilterGridColumn = styled.div`
    width: calc(50% - 48px);
    padding: 0 24px;
`;
